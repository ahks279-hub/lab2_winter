#include <stdio.h>

int add (int a, int b);

int main()
{
    int a, b;
    int sum;
  printf("Enter 2 numbers: ");
  scanf("%d %d",&a ,&b);
  
  sum=add(a,b);
  
;
  printf(" The sum is %d", sum);
  
  return 0;
}

int add (int a, int b){
    int total;
    
    total= a + b;
    return total;
}
